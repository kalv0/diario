* text=auto eol=lf

# El entrypoint se ejecuta dentro de una imagen Alpine: con CRLF el contenedor
# arranca con "no such file or directory" y cuesta media tarde averiguar por qué.
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
